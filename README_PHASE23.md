# Phase 23 — Combo + Pre Access + Routing Correction

Extract this archive directly inside `/opt/new-ai-bot`.

Included:
- 🎁 Combos visible in the admin panel.
- 🔓 Pre Access visible in the admin panel.
- Combo add/list/edit/delete/enable-disable.
- Multiple combo images, ordered album, add/delete/reorder.
- One common rich Telegram caption with embedded text links.
- Pre Access presets 1/2/3/4/5/10 minutes and custom duration.
- `/pre` exact saved Batch/Course resolution, one-person invite, temporary invite expiry, revoke-on-join, expiry removal/unban.
- Batch customer discovery remains AI-driven from authoritative batch name + keyword/business data.
- Courses remain exact normalized-name commands only.
- Shortcuts remain exact custom slash triggers only.
- No BM3 code/data/architecture is introduced.

Important: the archive contains `app/bot.py` at its top level so it replaces the actual running bot module.
